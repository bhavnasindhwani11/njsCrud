<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/stylesheets/posts.css">
    <title><%= title %></title>
</head>
<body>
    <div class="login-page">
        <div class="form">
            <% for(var i=0;i <comments.length; i++){%>
                <h1>User Who commented <%= comments[i].dataValues.user.username%></h1>
                <p>What user commented <strong><%= comments[i].dataValues.comment_desc%></strong></p>
                <% if(comments[i].dataValues.post.user_id == showCommentOptions){%>
                    <a href="/editComment/<%= comments[i].dataValues.id%>">Edit Comment</a>
                    <a href="/deleteComment/<%= comments[i].dataValues.id%>">Delete Comment</a>
                <%}%>
                <hr>
                <br>
            <%}%>
            <form action="/addComment" method="post">
                <input type="text" name="comment" id="comment" placeholder="comment Something" required>
                <input type="hidden" name="postId" value="<%= postId%>">
                <button type="submit">Add Comment</button>
            </form>
        </div>
    </div> 
</body>
</html>